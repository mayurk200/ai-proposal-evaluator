[project]
name = "agrieval-python-service"
version = "1.0.0"
description = "AI-powered document processing & proposal evaluation microservice"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
